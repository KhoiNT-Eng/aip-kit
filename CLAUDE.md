# CLAUDE.md

All project rules live in AGENTS.md (shared with Codex):

@AGENTS.md

## Claude-specific
- Talk to the user in Vietnamese; short, bullet-style answers. Double-check before claiming.
- This repo runs on the user's Mac: real `claude` sessions are running, so never test
  `aip acc use claude` against the real home directory. Use the test suites only.
